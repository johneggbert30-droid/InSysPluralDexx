window.APP_CONFIG = {
  // Leave backend auth off for a static-only publish.
  // When your API is live, set its URL here and switch useBackendAuth to true.
  apiBaseUrl: '',
  useBackendAuth: false
};
